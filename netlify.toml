[build]
  publish = "dist"
  command = "npm run build"

[[edge_functions]]
  path = "/blog/*"
  function = "og-blog"

# SPA fallback - serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
